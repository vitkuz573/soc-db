#!/usr/bin/env python3
"""
soc-db CLI — legacy shim that delegates to the installed package.

Usage:
    soc-db list
    soc-db query ...
    soc-db show <id>
    soc-db stats
    soc-db enrich
"""

import sys
from soc_db.cli import main

if __name__ == "__main__":
    sys.exit(main())
