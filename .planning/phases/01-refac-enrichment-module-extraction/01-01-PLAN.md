---
phase: 01-refac-enrichment-module-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/soc_db/enrich/__init__.py
  - src/soc_db/enrich/_vendor_data.py
  - src/soc_db/enrich/_helpers.py
  - src/soc_db/common.py
  - tests/snapshot/test_enrich_snapshot.py
  - .planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json
autonomous: true
requirements:
  - REFAC-01
  - REFAC-02
must_haves:
  truths:
    - ALL 1746 chips' enrichment output is captured as a deterministic reference before any refactoring
    - `from soc_db.enrich import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS` resolves without error
    - The enrich/ package is importable and passes ruff/mypy at `soc_db/` strictness level
    - Existing tests still pass — common.py unchanged in behavior
  artifacts:
    - src/soc_db/enrich/__init__.py
    - src/soc_db/enrich/_vendor_data.py
    - src/soc_db/enrich/_helpers.py
    - tests/snapshot/test_enrich_snapshot.py
    - .planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json
  key_links:
    - snapshot reference file must be committed before any extraction starts
    - common.py must continue to re-export VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS from enrich._vendor_data
---

<objective>
Capture a deterministic snapshot of ALL 1746 chips' enrichment output BEFORE any refactoring, and create the `src/soc_db/enrich/` package foundation with shared data constants and helpers.

Purpose: The snapshot test is the regression safety net for the entire phase. Without it, year-inference changes silently affect all 1746 chips. The enrich package structure is needed before extraction can begin.

Output:
1. `tests/snapshot/test_enrich_snapshot.py` — snapshot test that captures and compares enrichment output
2. `.planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json` — the reference output
3. `src/soc_db/enrich/__init__.py` — package with `from __future__ import annotations`
4. `src/soc_db/enrich/_vendor_data.py` — VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, MEMORY_CLOCK_FROM_TYPE
5. `src/soc_db/enrich/_helpers.py` — _has(), extract_int(), extract_freq(), extract_process(), clean()
6. Updated `common.py` with re-exports from enrich._vendor_data
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-refac-enrichment-module-extraction/01-CONTEXT.md
@src/soc_db/common.py
@src/soc_db/models.py
@tests/unit/test_common.py
@tests/property/test_enrich_one.py
@tests/benchmark/test_enrich_one.py
@scripts/migrate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create snapshot test and capture reference output</name>
  <files>
    tests/snapshot/test_enrich_snapshot.py
    .planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json
  </files>
  <action>
    Create `tests/snapshot/test_enrich_snapshot.py` that:

    1. Loads ALL chips from all vendor files in `data/` (excluding `index.json`) — same pattern as `tests/benchmark/test_enrich_one.py::load_all_chips()`.
    2. For each chip, runs `soc_db.common.enrich_one()` and serializes per-chip output to a deterministic JSON reference file.
    3. Uses `json.dumps(chip, sort_keys=True)` for deterministic ordering.
    4. Writes reference to `.planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json` as a list of dicts.
    5. The test function `test_enrich_snapshot_matches()` loads the reference file and asserts every chip matches.

    **Snapshot file format:** A single JSON array where each element is an enriched chip dict. The file is sorted by `(chip["vendor"], chip["id"])` for stable ordering. Use `ensure_ascii=False, indent=2` for git-diff-friendly output.

    **Reference capture mode:** Include a `--update-snapshot` marker so the maintainer can regenerate. On first run (reference missing), write the reference and pass. On subsequent runs, compare output to reference.

    **Key constraint:** The reference file MUST be captured before ANY code changes to `common.py`. The snapshot is the "before" state.
  </action>
  <verify>
    <automated>cd /home/vitaly/projects/soc-db && python -m pytest tests/snapshot/test_enrich_snapshot.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `expected_enrichment.json` exists with enrichment output for all vendor files
    - Snapshot test passes (first run creates reference, subsequent runs compare)
    - File is sorted deterministically by (vendor, id)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create enrich/ package with shared data modules</name>
  <files>
    src/soc_db/enrich/__init__.py
    src/soc_db/enrich/_vendor_data.py
    src/soc_db/enrich/_helpers.py
  </files>
  <action>
    Create `src/soc_db/enrich/` as a package.

    **`src/soc_db/enrich/__init__.py`:**
    - `from __future__ import annotations` — enables postponed evaluation to prevent circular imports
    - Re-export from submodules:
      - `from soc_db.enrich._vendor_data import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, MEMORY_CLOCK_FROM_TYPE, VENDOR_FILES`
      - `from soc_db.enrich._helpers import _has, clean`
    - Define `__all__ = ["VENDOR_KNOWLEDGE", "FIELD_GROUPS", "FIELD_WEIGHTS", "MEMORY_CLOCK_FROM_TYPE", "VENDOR_FILES", "_has", "clean"]`
    - No `TYPE_CHECKING` guards needed here (no type-only imports from sibling modules yet)

    **`src/soc_db/enrich/_vendor_data.py`:**
    - `from __future__ import annotations`
    - Move these constants verbatim from `src/soc_db/common.py`:
      - `VENDOR_FILES` (dict, lines 256-297)
      - `FIELD_GROUPS` (dict, lines 351-364)
      - `FIELD_WEIGHTS` (dict, lines 366-390)
      - `VENDOR_KNOWLEDGE` (dict, lines 392-702)
      - `MEMORY_CLOCK_FROM_TYPE` (dict, lines 721-731)
    - Keep exact same values — no semantic changes
    - Add module-level docstring describing what each constant contains

    **`src/soc_db/enrich/_helpers.py`:**
    - `from __future__ import annotations`
    - Move these utility functions verbatim from `src/soc_db/common.py`:
      - `_has(chip: dict[str, Any], field: str) -> bool` (lines 705-718)
      - `clean(text: str | None) -> str | None` (lines 94-112)
    - Import `re` for `_has` (uses no regex currently but `clean` does)
    - Copy `Any` from typing
    - Add module-level docstring

    **Do NOT touch `enrich_one()`, `enrich_all()`, `write_vendor_file()`, `slug()`, `fetch()`, `extract_model()`, `extract_int()`, `extract_freq()`, `extract_process()`, `merge_chips()`, `_match_existing()`, or `DATA_DIR`/`CACHE_DIR`/`USER_AGENT` — these remain in `common.py` for now.

    **Do NOT create the `src/soc_db/enrich/__pycache__/` directory** — gitignore handles it.
  </action>
  <verify>
    <automated>python -c "from soc_db.enrich import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, MEMORY_CLOCK_FROM_TYPE, _has, clean; print('OK')" && python -m pytest tests/unit/test_common.py -x -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `python -c "from soc_db.enrich import VENDOR_KNOWLEDGE"` succeeds
    - `python -c "from soc_db.enrich._helpers import _has, clean"` succeeds
    - Constants contain the same data as before (spot-check a few entries)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire common.py re-exports from enrich._vendor_data</name>
  <files>
    src/soc_db/common.py
  </files>
  <action>
    Update `src/soc_db/common.py` to import shared data from the new enrich modules instead of defining them inline:

    1. Add `from __future__ import annotations` at the top (after module docstring, before `import hashlib`)
    2. Replace the inline definitions of `VENDOR_FILES`, `FIELD_GROUPS`, `FIELD_WEIGHTS`, `VENDOR_KNOWLEDGE`, `MEMORY_CLOCK_FROM_TYPE` with re-exports:
       ```python
       from soc_db.enrich._vendor_data import (
           FIELD_GROUPS,
           FIELD_WEIGHTS,
           MEMORY_CLOCK_FROM_TYPE,
           VENDOR_FILES,
           VENDOR_KNOWLEDGE,
       )
       ```
    3. Replace the inline `_has()` function and `clean()` function with:
       ```python
       from soc_db.enrich._helpers import _has, clean
       ```
    4. Keep all other code in common.py unchanged.
    5. Ensure the import is placed where the removed definitions were (after `merge_chips` at line 349 for FIELD_GROUPS, after `VENDOR_KNOWLEDGE` etc.) — or more practically, place ALL re-exports as a block near the top after the stdlib imports, before `DATA_DIR`.
    6. Verify `__pycache__` is cleared before testing.

    **Nyquist gate:** After this change, run `python -c "from soc_db.common import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, MEMORY_CLOCK_FROM_TYPE, _has, clean, enrich_one, enrich_all, VENDOR_FILES"` — ALL old import paths must work.
  </action>
  <verify>
    <automated>
      python -c "from soc_db.common import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, MEMORY_CLOCK_FROM_TYPE, _has, clean, enrich_one, enrich_all, VENDOR_FILES" && \
      python -c "from soc_db.enrich import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS" && \
      python -m pytest tests/unit/test_common.py tests/property/test_enrich_one.py tests/benchmark/test_enrich_one.py tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - All `from soc_db.common import ...` import paths still work
    - All tests pass: unit, property, benchmark, snapshot
    - `from soc_db.enrich import VENDOR_KNOWLEDGE` works
    - `git diff --stat` shows common.py shrunk by ~350 lines
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrich package → common.py | Shared constants flowing across module boundary — must re-export identically |
| snapshot test → data files | Snapshot depends on unchanging source data — any chip data change invalidates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | _vendor_data.py | critical | mitigate | Constants copied verbatim, verified by import test — no semantic drift allowed |
| T-01-02 | Repudiation | snapshot test | high | mitigate | Reference file committed to git; `--update-snapshot` flag requires deliberate opt-in |
| T-01-03 | Information Disclosure | _helpers.py | low | accept | Internal helper functions; no sensitive data |
| T-01-SC | Tampering | pip installs | high | mitigate | No new dependencies — existing deps only; skip package legitimacy gate |

</threat_model>

<verification>
1. `python -m pytest tests/snapshot/test_enrich_snapshot.py -x -v` — snapshot passes before any extraction
2. `python -m pytest tests/unit/test_common.py tests/property/test_enrich_one.py tests/benchmark/test_enrich_one.py -x -q` — all existing tests pass
3. `python -c "from soc_db.common import VENDOR_KNOWLEDGE, FIELD_GROUPS, FIELD_WEIGHTS, enrich_one, enrich_all"` — all import paths work
4. `ruff check src/soc_db/enrich/` — no new lint errors
5. `mypy src/soc_db/enrich/` — type-checks cleanly (exemptions from pyproject.toml for scripts/tests still apply)
</verification>

<success_criteria>
- expected_enrichment.json contains 1746+ enriched chips, committed to git
- enrich/ package is importable and all submodules type-check
- common.py re-exports all moved constants — backward compat intact
- All tests pass: unit, property, benchmark, snapshot
- Snapshot reference is the "before" state; subsequent plans compare against it
</success_criteria>

<output>
Create `.planning/phases/01-refac-enrichment-module-extraction/01-01-SUMMARY.md` when done
</output>
