---
phase: 01-refac-enrichment-module-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/soc_db/enrich/__init__.py
  - src/soc_db/enrich/modem.py
  - src/soc_db/enrich/npu.py
  - src/soc_db/enrich/connectivity.py
  - src/soc_db/enrich/storage.py
  - src/soc_db/common.py
autonomous: false
requirements:
  - REFAC-01
must_haves:
  truths:
    - Modem inference produces identical results for ALL chips (snapshot test passes)
    - NPU inference produces identical results for ALL chips
    - Wi-Fi and Bluetooth inference from year produces identical results
    - Storage type inference from year produces identical results
  artifacts:
    - src/soc_db/enrich/modem.py
    - src/soc_db/enrich/npu.py
    - src/soc_db/enrich/connectivity.py
    - src/soc_db/enrich/storage.py
  key_links:
    - Year must be inferred BEFORE modem/npu/connectivity/storage (they all depend on chip["year"])
    - Modem/npu inference depends on VENDOR_KNOWLEDGE (imported from _vendor_data)
    - Connectivity and storage are pure year-based lookups
---

<objective>
Extract the four peripheral enrichment domains: modem, NPU, connectivity (Wi-Fi/BT), and storage type. These are year-dependent — they run after year inference.

Purpose: Complete the per-domain extraction pattern for all year-dependent enrichment steps. Each module is independently unit-testable and documented.

Output:
1. `src/soc_db/enrich/modem.py` — modem inference per vendor/year
2. `src/soc_db/enrich/npu.py` — NPU inference per vendor/year/model
3. `src/soc_db/enrich/connectivity.py` — Wi-Fi and Bluetooth from year
4. `src/soc_db/enrich/storage.py` — storage type from year
5. Updated common.py — enrich_one() delegates to all four modules
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refac-enrichment-module-extraction/01-02-SUMMARY.md
@src/soc_db/common.py
@src/soc_db/enrich/__init__.py
@src/soc_db/enrich/year.py
@src/soc_db/enrich/gpu.py
@src/soc_db/enrich/process.py
@src/soc_db/enrich/memory.py
@src/soc_db/enrich/_vendor_data.py
@src/soc_db/enrich/_helpers.py
@tests/unit/test_common.py
@tests/snapshot/test_enrich_snapshot.py
@.planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract modem and NPU modules</name>
  <files>
    src/soc_db/enrich/modem.py
    src/soc_db/enrich/npu.py
    src/soc_db/common.py
    src/soc_db/enrich/__init__.py
  </files>
  <action>
    Create both modules. They share no dependency on each other but both depend on `VENDOR_KNOWLEDGE` from `_vendor_data`.

    **`src/soc_db/enrich/modem.py`:**
    - `from __future__ import annotations`
    - `import re`
    - `from typing import Any`
    - `from soc_db.enrich._vendor_data import VENDOR_KNOWLEDGE`
    
    Define `infer_modem(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the modem inference block from lines 1446-1488 of common.py verbatim
    - This covers Qualcomm SM/SDM → Snapdragon X-series modems (lines 1451-1470)
    - Qualcomm Snapdragon N Gen M → modem (lines 1460-1466)
    - Qualcomm year-based 5G/4G LTE fallback (lines 1467-1470)
    - MediaTek Dimensity/MT → 5G/4G (lines 1471-1476)
    - Samsung Exynos → 5G/4G (lines 1477-1481)
    - Apple → Apple 5G (lines 1482-1483)
    - HiSilicon Kirin → Balong 5G/4G LTE (lines 1484-1488)
    - Mutates chip in-place, returns chip
    
    The modem inference depends on `chip["year"]` being set. The `if not chip.get("modem")` guard makes subsequent calls safe.

    **`src/soc_db/enrich/npu.py`:**
    - `from __future__ import annotations`
    - `import re`
    - `from typing import Any`
    - `from soc_db.enrich._vendor_data import VENDOR_KNOWLEDGE`
    
    Define `infer_npu(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the NPU inference block from lines 1424-1445 of common.py verbatim
    - This covers Apple Neural Engine (line 1431)
    - Qualcomm SM7/SM8/SM6 → Hexagon NPU (lines 1432-1434)
    - MediaTek Dimensity → APU, MT>=8000 → APU (lines 1435-1441)
    - Samsung Exynos 21/22/24/25 → Samsung NPU (line 1442-1443)
    - HiSilicon Kirin 9/8/7 → HiSilicon NPU (lines 1444-1445)
    - Mutates chip in-place, returns chip
    
    The NPU inference depends on `chip["year"]` being >= 2017. The `if not chip.get("npu")` guard provides idempotency.

    **Update `common.py`:**
    - Add `from soc_db.enrich.modem import infer_modem` and `from soc_db.enrich.npu import infer_npu`
    - Replace the modem inference lines (1446-1488) with `infer_modem(chip)`
    - Replace the NPU inference lines (1424-1445) with `infer_npu(chip)`
    - Ensure calls are placed at same position in the pipeline (after GPU fallback, before Wi-Fi)

    **Update `src/soc_db/enrich/__init__.py`:**
    - Add `from soc_db.enrich.modem import infer_modem` and `from soc_db.enrich.npu import infer_npu`
    - Add to `__all__`
  </action>
  <verify>
    <automated>
      python -m pytest tests/unit/test_common.py::TestModemInference tests/unit/test_common.py::TestNPUInference tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - All ModemInference and NPUInference parametrized tests pass
    - Snapshot test passes (identical output to reference)
    - Both modules are importable and type-check cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract connectivity and storage modules</name>
  <files>
    src/soc_db/enrich/connectivity.py
    src/soc_db/enrich/storage.py
    src/soc_db/common.py
    src/soc_db/enrich/__init__.py
  </files>
  <action>
    **`src/soc_db/enrich/connectivity.py`:**
    - `from __future__ import annotations`
    - `from typing import Any`
    
    Define `infer_wifi(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the Wi-Fi inference from lines 1490-1504 of common.py verbatim
    - The `wifi_by_year` threshold table must be verbatim
    - Mutates chip in-place, returns chip
    
    Define `infer_bluetooth(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the Bluetooth inference from lines 1505-1521 of common.py verbatim
    - The `bt_by_year` threshold table must be verbatim
    - Mutates chip in-place, returns chip

    Both functions depend on `chip["year"]` being set and check `if not chip.get("wifi")` / `if not chip.get("bluetooth")`.

    **`src/soc_db/enrich/storage.py`:**
    - `from __future__ import annotations`
    - `from typing import Any`
    
    Define `infer_storage(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the storage type inference from lines 1366-1377 of common.py verbatim
    - The `st_by_year` threshold table must be verbatim
    - Mutates chip in-place, returns chip

    **Update `common.py`:**
    - Add imports: `from soc_db.enrich.connectivity import infer_wifi, infer_bluetooth` and `from soc_db.enrich.storage import infer_storage`
    - Replace Wi-Fi lines (1490-1504) with `infer_wifi(chip)`
    - Replace Bluetooth lines (1505-1521) with `infer_bluetooth(chip)`
    - Replace storage lines (1366-1377) with `infer_storage(chip)`
    - Keep the call order: storage (after memory type from year), then modem, then NPU, then Wi-Fi, then Bluetooth

    **Update `src/soc_db/enrich/__init__.py`:**
    - Add re-exports for `infer_wifi`, `infer_bluetooth`, `infer_storage`
    - Update `__all__`
  </action>
  <verify>
    <automated>
      python -m pytest tests/unit/test_common.py::TestMemoryInference tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - Wi-Fi, Bluetooth, and storage type tests from TestMemoryInference pass
    - Snapshot test passes (identical output to reference)
    - All year-dependent domain modules are now extracted
  </done>
</task>

<task type="auto">
  <name>Task 3: Run comprehensive verification across all domain modules</name>
  <files>
    src/soc_db/enrich/__init__.py
  </files>
  <action>
    Run the full test suite to verify no regression:

    1. `python -m pytest tests/snapshot/test_enrich_snapshot.py -x -v` — snapshot must pass
    2. `python -m pytest tests/unit/test_common.py -x -v -q` — all unit tests pass
    3. `python -m pytest tests/property/test_enrich_one.py -x -v -q` — property tests pass
    4. `python -m pytest tests/benchmark/test_enrich_one.py -x -v -q` — benchmark tests pass
    5. `ruff check src/soc_db/enrich/` — no lint errors
    6. `mypy src/soc_db/enrich/` — type-checks cleanly

    If any test fails, fix before proceeding. Common issues and fixes:
    
    - **ImportError/circular import**: Add `TYPE_CHECKING` guard in the module that imports from a sibling. Use `from __future__ import annotations` and `if TYPE_CHECKING: from soc_db.enrich._vendor_data import ...` — but only if actually circular. Current module structure should not have circular deps (year → nothing, gpu → _vendor_data only, process → _vendor_data only, memory → _vendor_data only, modem → _vendor_data only, npu → _vendor_data only, connectivity → nothing, storage → nothing).
    
    - **Snapshot mismatch**: The reference JSON may have been captured with a different Python version or data file state. Check if any data files changed. If legitimate data change occurred, regenerate with `--update-snapshot`.

    - **Type error in mypy**: Add type annotations to function signatures. All functions take `dict[str, Any]` and return `dict[str, Any]`.
  </action>
  <verify>
    <automated>
      python -m pytest tests/ -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - ALL tests pass: unit, property, benchmark, snapshot
    - ruff check passes on src/soc_db/enrich/
    - mypy passes on src/soc_db/enrich/
    - enrich_one() still produces identical output for ALL 1746 chips
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrich/modules → common.py | All domain functions cross module boundary — must be called in correct order |
| enrich/modules → _vendor_data | Shared constants dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | modem.py vendor/year logic | medium | mitigate | Copy verbatim from common.py; snapshot test verifies |
| T-01-09 | Tampering | npu.py vendor logic | medium | mitigate | Copy verbatim from common.py; snapshot test verifies |
| T-01-10 | Tampering | connectivity.py year tables | low | mitigate | Threshold tables copied verbatim; trivial logic |
| T-01-11 | Denial of Service | module import order | low | mitigate | No circular dependencies in current structure; TYPE_CHECKING guards ready if needed |
| T-01-SC | Tampering | pip installs | high | mitigate | No new dependencies; skip package legitimacy gate |

</threat_model>

<verification>
1. After each task: `python -m pytest tests/snapshot/test_enrich_snapshot.py -x` — must pass
2. Final: `python -m pytest tests/ -x --tb=short -q` — full suite
3. `ruff check src/soc_db/enrich/ src/soc_db/common.py`
4. `mypy src/soc_db/enrich/`
</verification>

<success_criteria>
- All eight domain modules exist and are importable: year, gpu, process, memory, modem, npu, connectivity (wifi+bt), storage
- All modules type-check with mypy strict
- enrich_one() in common.py delegates all domain logic to module functions
- Snapshot test passes — zero regression on all 1746 chips
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/01-refac-enrichment-module-extraction/01-03-SUMMARY.md` when done
</output>
