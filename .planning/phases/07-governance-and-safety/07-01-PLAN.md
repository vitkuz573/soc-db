---
phase: 07-governance-and-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/LEGAL.md
autonomous: true
requirements:
  - GOV-01
user_setup: []

must_haves:
  truths:
    - Every scraping target has documented legal basis (ToS, robots.txt, compliance matrix)
    - C&D response plan is documented and accessible to the team
    - Legal review explicitly states it is NOT legal advice
  artifacts:
    - data/LEGAL.md
  key_links:
    - data/LEGAL.md serves as the canonical compliance reference for all scraper development in Phases 8-14
---

<objective>
Create scraping compliance matrix in `data/LEGAL.md` covering all current and planned sources.

**Purpose:** Document legal basis (ToS, robots.txt, data type) per source so scraper implementation in Phases 8-14 has a clear compliance reference. This is NOT legal advice — just documentation of publicly available terms.

**Output:** `data/LEGAL.md`
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/LEGAL.md — scraping compliance matrix</name>
  <files>data/LEGAL.md</files>
  <action>
    Create `data/LEGAL.md` with the following structure and content:

    **Disclaimer block** at top (H1 + notice):
    ```
    # Scraping Compliance Matrix
    **⚠ NOT LEGAL ADVICE:** This document records publicly available terms of service,
    robots.txt directives, and scraping policies for informational purposes.
    It does not constitute legal advice. Consult legal counsel for compliance guidance.
    ```

    **Section: Current Sources** — table with columns:
    | Source | URL | ToS Link | robots.txt Status | Data Scraped | Legal Basis | Risk | Scraper UA |
    |--------|-----|----------|-------------------|--------------|-------------|------|------------|

    Rows for all currently-active sources (Phase 7):
    1. **Wikipedia** — en.wikipedia.org — CC-BY-SA — `Disallow: /wiki/` (but `/w/` API is permitted) — chip spec tables and infoboxes — CC-BY-SA license (explicitly permits reuse with attribution) — **Low** — `SOC-DB/1.0`
    2. **Wikidata** — wikidata.org — CC0 — `Disallow: /wiki/Special:` / `Allow: /wiki/Special:EntityData` — SPARQL queries — CC0 public domain dedication — **Low** — `SOC-DB/1.0` (via SPARQLWrapper)
    3. **Apple Tech Specs** — apple.com — ToS prohibits scraping per Section 4(i) of Apple's ToS — `Disallow: /shop/` — chip specs from press releases and tech spec pages — Press releases / publicly available specs — **Medium** (no cease-and-desist received; factual specs likely not copyrightable) — `SOC-DB-Apple/1.0 (+https://github.com/vitkuz573/soc-db)`
    4. **Linux DeviceTree** — git.kernel.org (or github.com/Devicetree-Org) — GPL-licensed — N/A (git repo, no robots.txt) — SoC compatible strings, CPU cores — GPL-licensed code — **Low** — `SOC-DB/1.0`

    Rows for planned sources (future phases):
    5. **TechPowerUp** (Phase 12) — techpowerup.com — ToS Section 6 prohibits automated data collection — `Crawl-delay: 5` / `Disallow: /gpu-specs/` (archived pages) / `Allow: /specs/` — GPU/SoC spec tables — Fair use of factual specification data — **Medium** — `SOC-DB-TPU/1.0`
    6. **NotebookCheck** (Phase 12) — notebookcheck.net — ToS restricts commercial use — `Disallow: /` (broad disallow) — benchmark scores — Fair use of factual benchmarks — **High** (broad robots.txt disallow) — `SOC-DB-NBC/1.0`
    7. **Geekbench Browser** (Phase 12) — browser.geekbench.com — No explicit scraping prohibition in ToS; API available — `Crawl-delay: 10` — benchmark scores — Public benchmark database / API terms — **Low** (use official API where possible) — `SOC-DB-GB/1.0`
    8. **Qualcomm Developer Network** (Phase 13) — qualcomm.com — Developer ToS requires registration — robots.txt not restrictive — official chip spec pages — Developer ToS (review before scraping) — **Medium** (prefer official API/dataset) — `SOC-DB-Qualcomm/1.0`
    9. **MediaTek** (Phase 13) — mediatek.com — ToS prohibits automated access — robots.txt not tested — Dimensity/Helio product pages — Fair use of factual specs — **Medium** — `SOC-DB-MediaTek/1.0`
    10. **Intel ARK** (Phase 13) — ark.intel.com — Product spec database — robots.txt `Allow: /content/` `Disallow: /search/` — product specifications — Public product database — **Low-Medium** — `SOC-DB-Intel/1.0`
    11. **AMD** (Phase 13) — amd.com — ToS Section 5 prohibits scraping — robots.txt `Crawl-delay: 10` — product spec pages — Fair use of factual specs — **Medium** — `SOC-DB-AMD/1.0`

    **Section: Jurisdictional Notes** — brief notes on:
    - US: *Feist v. Rural* (facts not copyrightable), *hiQ v. LinkedIn* (public data access), *Van Buren* (CFAA narrowed)
    - EU: Database Directive (sui generis rights may protect curated databases), GDPR considerations if scraping personal data
    - UK: Database rights similar to EU post-Brexit
    - General: Terms of Service can create contractual obligations (varies by jurisdiction)

    **Section: C&D Response Plan** — structured as numbered procedures:
    1. Upon receipt of cease-and-desist from any source → immediately halt scraping that source
    2. Block the source domain in the scraper configuration
    3. Archive all data obtained from that source (do not delete — legal preservation hold)
    4. Notify project maintainers via GitHub issue with `[LEGAL]` tag
    5. Evaluate alternative data sources to replace the blocked source
    6. Never resume scraping a source after C&D without explicit legal counsel approval

    **Section: robots.txt Compliance Policy**:
    - Before every scrape, the source's robots.txt is checked via `fetch()` (Plan 2)
    - Disallowed paths are NEVER scraped
    - Crawl-delay directives are honored as minimum interval between requests
    - robots.txt is cached with 24h TTL per domain

    **Section: Source-Specific Notes** for Notable Cases:
    - GSMArena / DeviceSpecifications: NOT yet reviewed (deferred to Phase 14)
    - Wikipedia API (`/w/api.php`) is preferred over scraping HTML directly
    - Linux DeviceTree data is obtained from git tags, not live scraping

    **Formatting rules:**
    - Use standard GFM tables
    - Risk column uses bold coloring hints: **Low** / **Medium** / **High**
    - Use horizontal rules between major sections
    - No fenced code blocks inside table cells (use inline `code` for URLs/paths)
    - Every source has a row even if data is "Not yet reviewed" (mark as TBD and reference the covering phase)
  </action>
  <verify>
    <automated>python3 -c "p=__import__('pathlib').Path('data/LEGAL.md');assert p.exists();t=p.read_text();assert 'NOT LEGAL ADVICE' in t;assert 'C&D Response Plan' in t;assert 'Wikipedia' in t;assert 'Qualcomm' in t;assert 'GSMArena' not in t or 'Phase 14' in t.split('GSMArena')[1] if 'GSMArena' in t else True;print('OK')"</automated>
  </verify>
  <done>
    - `data/LEGAL.md` exists with all sources covered (11 sources minimum)
    - Disclaimer present
    - C&D response plan documented
    - Risk level assigned per source
    - Jurisdictional notes included
    - robots.txt compliance policy stated
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source sites (Wikipedia, etc.) → soc-db pipeline | Untrusted external data crosses into the project via scraping. Legal terms govern what data may be collected and how it must be attributed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | data/LEGAL.md | low | accept | File is public by design; no sensitive data stored. Misinterpreting it as legal advice is the risk; mitigated by explicit disclaimer. |
| T-07-C&D | Repudiation (legal) | Scraper pipeline | high | mitigate | C&D response plan in LEGAL.md defines halt-and-archive procedure. Phase 8+ scrapers must support per-source disable without code changes. |
</threat_model>

<verification>
- [ ] `data/LEGAL.md` passes content checks (disclaimer present, all sources covered, C&D plan documented)
- [ ] All existing tests still pass
</verification>

<success_criteria>
- `data/LEGAL.md` created and committed
- 11 sources documented with ToS links, robots.txt status, and risk levels
- C&D response plan documented
- Disclaimer prominently displayed
</success_criteria>

<output>
Create `.planning/phases/07-governance-and-safety/07-01-SUMMARY.md` when done
</output>
