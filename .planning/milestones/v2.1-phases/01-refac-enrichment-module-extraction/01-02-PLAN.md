---
phase: 01-refac-enrichment-module-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/soc_db/enrich/__init__.py
  - src/soc_db/enrich/year.py
  - src/soc_db/enrich/gpu.py
  - src/soc_db/enrich/process.py
  - src/soc_db/enrich/memory.py
  - src/soc_db/common.py
autonomous: false
requirements:
  - REFAC-01
must_haves:
  truths:
    - Year inference produces IDENTICAL results for ALL 1746 chips (snapshot test passes)
    - Year inference priority ordering is explicitly documented in year.py
    - GPU inference produces identical results via VENDOR_KNOWLEDGE.gpu_map + vendor defaults
    - Process node inference produces identical results via VENDOR_KNOWLEDGE.process_map + year-based fallback
    - Memory inference produces identical results (type, clock, bus)
  artifacts:
    - src/soc_db/enrich/year.py
    - src/soc_db/enrich/gpu.py
    - src/soc_db/enrich/process.py
    - src/soc_db/enrich/memory.py
  key_links:
    - enrich_one() in common.py must call domain functions in exact original order
    - Year inference must run BEFORE process/memory year-based fallbacks
    - GPU VENDOR_KNOWLEDGE lookup must run BEFORE year-dependent vendor defaults
---

<objective>
Extract the four core enrichment domains into separate modules: year, process, GPU, memory. Each module gets a dedicated `infer_*()` function called from `enrich_one()` in order-preserving fashion.

Purpose: Break the 700-line monolithic function into testable, documented domain modules. Year inference is the highest-risk extraction (530 lines, 22+ regex branches, implicit priority chain).

Output:
1. `src/soc_db/enrich/year.py` — documented priority-ordered year inference chain
2. `src/soc_db/enrich/gpu.py` — GPU lookup + vendor defaults
3. `src/soc_db/enrich/process.py` — process node from VENDOR_KNOWLEDGE + year fallback
4. `src/soc_db/enrich/memory.py` — memory type, clock, bus inference
5. Updated `common.py` — enrich_one() delegates to module functions
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-refac-enrichment-module-extraction/01-01-SUMMARY.md
@src/soc_db/common.py
@src/soc_db/enrich/__init__.py
@src/soc_db/enrich/_vendor_data.py
@src/soc_db/enrich/_helpers.py
@tests/unit/test_common.py
@tests/property/test_enrich_one.py
@tests/benchmark/test_enrich_one.py
@tests/snapshot/test_enrich_snapshot.py
@.planning/phases/01-refac-enrichment-module-extraction/expected_enrichment.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract year inference into enrich/year.py with documented priority chain</name>
  <files>
    src/soc_db/enrich/year.py
    src/soc_db/common.py
    src/soc_db/enrich/__init__.py
  </files>
  <behavior>
    - Test A: MT9999 → 2025, MT9899 → 2024, MT9200 → 2023 (MediaTek MT priority chain preserved)
    - Test B: DIMENSITY 9400 → 2025, DIMENSITY 9300 → 2024 (Dimensity chain preserved)
    - Test C: SM8750 → 2025, SM8650 → 2024 (Qualcomm SM chain preserved)
    - Test D: EXYNOS 2500 → 2025, EXYNOS 2400 → 2024 (Samsung Exynos chain preserved)
    - Test E: KIRIN 9010 → 2024, KIRIN 9000 → 2020 (HiSilicon Kirin chain preserved)
    - Test F: RK3288 → 2012, Snapdragon X Elite → 2024 (minor patterns preserved)
    - Test G: Empty/None model → no year inferred (None returned)
    - Test H: All parametrized TestYearInference cases pass
  </behavior>
  <action>
    Create `src/soc_db/enrich/year.py`:

    1. `from __future__ import annotations`
    2. `import re`
    3. `from typing import Any`
    4. `from soc_db.enrich._helpers import _has` (if needed; year module doesn't use _has currently)

    5. **Define `infer_year(chip: dict[str, Any]) -> int | None`:**
       Extract the year inference block from `common.py` lines 795-1328 as a single function.
       
       **Critical: Preserve EXACT order of if/elif/break chain.** The year inference block is:
       - Year validation (lines 795-797)
       - Main inference loop (lines 799-1328):
         - MT\d{4} pattern (MediaTek)
         - DIMENSITY/HELIO pattern
         - KIRIN pattern
         - SM/SDM pattern (Qualcomm)
         - MSM/APQ pattern
         - EXYNOS pattern
         - SNAPDRAGON X ELITE pattern
         - SNAPDRAGON X 2 pattern
         - SNAPDRAGON N GEN M pattern
         - RK\d{4} pattern (Rockchip)
         - [AM]\d+ pattern (Allwinner)
         - TEGRA pattern
         - T-prefix pattern
         - ... all remaining patterns in their original order

       **Architecture of the function:**
       ```python
       def infer_year(chip: dict[str, Any]) -> int | None:
           """Infer release year from chip model/name.
           
           Priority chain (checked in order, first match wins):
           1. MediaTek MT\d{4} model pattern
           2. MediaTek DIMENSITY/HELIO pattern  
           3. HiSilicon KIRIN pattern
           4. Qualcomm SM/SDM pattern
           5. Qualcomm MSM/APQ pattern
           6. Samsung Exynos pattern
           7. Qualcomm Snapdragon X Elite/Plus
           8. Qualcomm Snapdragon X2
           9. Qualcomm Snapdragon N Gen M
           10. Rockchip RK\d{4}
           11. Allwinner A/M prefix pattern
           12. Nvidia Tegra
           13. Intel T-prefix
           14. ... (document ALL branches in order)
           
           Args:
               chip: The chip record with model and name fields.
           
           Returns:
               The inferred year as int, or None if no pattern matched.
           """
       ```
       
       The complete priority chain docstring is MANDATORY — it documents what was implicit.

    6. **Handle the `year` field in the caller, not in `infer_year()`:**
       - `infer_year()` should only do the inference logic (lines 799-1328)
       - It should NOT set `chip["year"]` — it returns `int | None`
       - The caller (`enrich_one()`) sets `chip["year"]` based on the return value

    7. **Update `enrich_one()` in `common.py`:**
       - Add `from soc_db.enrich.year import infer_year` at the top of common.py (with TYPE_CHECKING guard if circular, but year module doesn't import from common.py so this is safe)
       - Replace lines 795-1328 with:
         ```python
         year = chip.get("year")
         if not year:
             inferred = infer_year(chip)
             if inferred:
                 chip["year"] = inferred
         ```
       - Keep the year validation (lines 795-797) in common.py before the inference call
       - Keep the `if year: chip["year"] = year` at line 1329-1330

    8. **Update `src/soc_db/enrich/__init__.py`:**
       - Add `from soc_db.enrich.year import infer_year`
       - Add `"infer_year"` to `__all__`

    **After extraction:** Run snapshot test immediately. If any year differs, debug and fix — do NOT proceed.
  </action>
  <verify>
    <automated>
      python -c "from soc_db.enrich import infer_year; chip = {'model': 'SM8550', 'name': 'Snapdragon 8 Gen 2'}; y = infer_year(chip); assert y == 2023, f'Expected 2023, got {y}'" && \
      python -m pytest tests/unit/test_common.py::TestYearInference tests/unit/test_common.py::TestEnrichOne tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - All 130+ parametrized year inference tests pass
    - ALL parametrized test cases in TestYearInference produce exact expected years
    - Snapshot test passes — ALL 1746 chips have identical year as before extraction
    - Priority chain is documented in year.py docstring with numbered list
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract process node and memory modules</name>
  <files>
    src/soc_db/enrich/process.py
    src/soc_db/enrich/memory.py
    src/soc_db/common.py
    src/soc_db/enrich/__init__.py
  </files>
  <action>
    Create two domain modules in parallel (no cross-dependency between them).

    **`src/soc_db/enrich/process.py`:**
    - `from __future__ import annotations`
    - `from typing import Any`
    - `from soc_db.enrich._vendor_data import VENDOR_KNOWLEDGE`
    
    Define `infer_process(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the VENDOR_KNOWLEDGE.process_map lookup (lines 784-789)
    - Extract the year-based process node fallback (lines 1332-1349)
    - Mutates chip in-place, returns chip
    - Both passes in one function — first tries vendor knowledge, then year-based fallback if process_nm still unset
    
    `infer_process()` must IDENTICALLY replicate the two-pass behavior: first check VENDOR_KNOWLEDGE.process_map by model, then (if still no process_nm) use year threshold table. The year threshold table from common.py lines 1333-1349 must be verbatim.

    **`src/soc_db/enrich/memory.py`:**
    - `from __future__ import annotations`
    - `from typing import Any`
    - `from soc_db.enrich._vendor_data import MEMORY_CLOCK_FROM_TYPE`
    
    Define `infer_memory(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract the memory clock from type initial lookup (lines 775-778)
    - Extract the memory bus from type initial lookup (lines 780-781)
    - Extract the memory type from year fallback (lines 1351-1364)
    - Extract the memory clock 2nd pass (lines 1378-1393)
    - Extract the memory bus 2nd pass (lines 1394-1398)
    - Mutates chip in-place, returns chip
    
    `infer_memory()` must preserve the exact year-based LPDDR mapping table from common.py lines 1353-1363 and the clock/bus lookup tables.

    **Update `common.py`:**
    - Add imports: `from soc_db.enrich.process import infer_process` and `from soc_db.enrich.memory import infer_memory`
    - Replace extracted line ranges with calls to `infer_process(chip)` and `infer_memory(chip)` at the same positions in the pipeline

    **Update `src/soc_db/enrich/__init__.py`:**
    - Add re-exports for `infer_process`, `infer_memory`
    - Update `__all__`
  </action>
  <verify>
    <automated>
      python -m pytest tests/unit/test_common.py tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - Snapshot test passes (identical output to reference)
    - All unit tests pass (process_nm tests, memory tests, year inference tests)
    - Process node and memory module functions are importable
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract GPU module and wire all four modules into common.py</name>
  <files>
    src/soc_db/enrich/gpu.py
    src/soc_db/common.py
    src/soc_db/enrich/__init__.py
  </files>
  <action>
    **`src/soc_db/enrich/gpu.py`:**
    - `from __future__ import annotations`
    - `from typing import Any`
    - `from soc_db.enrich._vendor_data import VENDOR_KNOWLEDGE`
    
    Define `infer_gpu(chip: dict[str, Any]) -> dict[str, Any]`:
    - Extract VENDOR_KNOWLEDGE.gpu_map lookup (lines 790-794)
    - Extract vendor-default GPU fallback (lines 1399-1423)
    - Mutates chip in-place, returns chip
    
    The function must handle both passes: first try VENDOR_KNOWLEDGE.gpu_map by model, then (if still no gpu) use the vendor/year-based defaults table. The vendor defaults table (lines 1402-1423) with `gpu_by_vendor` dict and the MediaTek/Qualcomm/Samsung/HiSilicon/Unisoc/NXP/Xilinx fallbacks must be verbatim.

    **Update `common.py`:**
    - Add `from soc_db.enrich.gpu import infer_gpu` import
    - Add call to `infer_gpu(chip)` after the memory block (same position as original code)
    - The GPU VENDOR_KNOWLEDGE lookup goes BEFORE year inference (at line ~790)
    - The GPU vendor-default fallback goes AFTER year inference (at line ~1399)
    - The function handles both passes internally via the `if not chip.get("gpu")` guard

    **Final wire-up check:**
    After this task, `enrich_one()` in `common.py` should:
    1. Clean name/model (lines 761-766) — still inline
    2. Model fallback (lines 767-772) — still inline
    3. Get VENDOR_KNOWLEDGE for vendor (line 773) — still inline
    4. Call `infer_memory(chip)` — first pass clock/bus from type
    5. Set architecture from VENDOR_KNOWLEDGE (lines 782-783) — still inline
    6. Call `infer_process(chip)` — first pass from vendor knowledge
    7. Call `infer_gpu(chip)` — first pass from vendor knowledge
    8. Year validation (lines 795-797) — still inline
    9. Call `infer_year(chip)` — year inference
    10. Call `infer_process(chip)` — second pass from year
    11. Call `infer_memory(chip)` — second pass type/clock/bus from year
    12. Call `infer_gpu(chip)` — second pass vendor defaults from year
    13. ... (modem, npu, etc still inline for now)
    
    Each function is idempotent — calling it twice is safe because of `if not chip.get("field")` guards.
  </action>
  <verify>
    <automated>
      python -m pytest tests/unit/test_common.py::TestGPUInference tests/unit/test_common.py::TestEnrichOne tests/snapshot/test_enrich_snapshot.py -x --tb=short -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - GPU test cases pass (both VENDOR_KNOWLEDGE lookup and vendor defaults)
    - Snapshot test passes — ALL 1746 chips produce identical output
    - All unit, property, and benchmark tests pass
    - Four domain modules are fully wired: year, process, memory, gpu
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrich year module → common.py | Year inference function crosses module boundary — must maintain exact priority ordering |
| enrich domain modules → enrich_one() | Calling order dependency — year must be inferred before process/memory/gpu fallbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | year.py inference chain | critical | mitigate | Snapshot test after EACH extraction; year priority chain explicitly documented in module docstring |
| T-01-05 | Tampering | process.py year-based fallback | high | mitigate | Year threshold table copied verbatim; snapshot test verifies |
| T-01-06 | Tampering | gpu.py vendor defaults | high | mitigate | gpu_by_vendor dict copied verbatim from common.py |
| T-01-07 | Elevation of Privilege | gpu.py two-pass | medium | mitigate | Idempotent design — `if not chip.get("gpu")` guards ensure second pass doesn't override first |
| T-01-SC | Tampering | pip installs | high | mitigate | No new dependencies; skip package legitimacy gate |

</threat_model>

<verification>
1. After EACH task in this plan, run `python -m pytest tests/snapshot/test_enrich_snapshot.py -x`
2. If ANY task causes a snapshot mismatch, STOP and fix before proceeding
3. Run full test suite at end: `python -m pytest tests/ -x --tb=short -q`
4. Verify `ruff check src/soc_db/enrich/` passes
5. Verify `mypy src/soc_db/enrich/` passes
</verification>

<success_criteria>
- All four domain modules exist with documented functions
- Year inference priority chain is explicitly documented
- Snapshot test passes (zero regression vs reference)
- Unit tests, property tests, and benchmark tests all pass
- common.py's enrich_one() delegates to module functions while preserving exact pipeline order
</success_criteria>

<output>
Create `.planning/phases/01-refac-enrichment-module-extraction/01-02-SUMMARY.md` when done
</output>
