---
phase: 10-dedup-and-identity
plan: 03
type: tdd
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - tests/unit/test_dedup.py
autonomous: true
requirements:
  - DEDUP-01
  - DEDUP-02
  - DEDUP-03

must_haves:
  truths:
    - DedupEngine UUID generation is tested for determinism, format, and backward compatibility
    - All 5 matching strategies (exact, alias, regex, Wikidata QID, fuzzy) have test coverage
    - write_vendor_file dedup integration is tested (new chips, updates, no-false-positive)
    - scripts/ deletion is verified (no import breaks)
    - All existing tests still pass
  artifacts:
    - tests/unit/test_dedup.py
  key_links:
    - DedupEngine.chip_uuid determinism verified
    - Match strategy ordering verified (exact -> alias -> regex -> Wikidata -> fuzzy)
    - write_vendor_file with DedupEngine produces correct merged output
---
<objective>
Comprehensive test coverage for the DedupEngine, write_vendor_file integration, and scripts/ migration.

**Purpose:** Ensure the canonical UUID identity system, multi-strategy matcher, and scripts/ consolidation all work correctly without breaking backward compatibility. All 405+ existing tests must continue to pass.

**Output:** `tests/unit/test_dedup.py` with 30+ test cases covering all strategies, edge cases, and integration.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/soc_db/dedup.py
@src/soc_db/common.py
@src/soc_db/models.py
@tests/conftest.py
@tests/unit/test_common.py
@tests/unit/test_scraping_base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DedupEngine unit tests — UUID generation, alias registry, 5 matching strategies</name>
  <files>
    tests/unit/test_dedup.py
  </files>
  <behavior>
    - Test 1: chip_uuid("Qualcomm", "SM8550") returns same 32-char hex string on every call
    - Test 2: chip_uuid("qualcomm", "sm8550") returns same as capitalized version (case-insensitive)
    - Test 3: chip_uuid("Qualcomm", "") falls back to name-based slug; still returns 32-char hex
    - Test 4: DedupEngine.match() -> exact_model match returns correct existing ID
    - Test 5: DedupEngine.match() -> alias match (e.g., "Kalama" matches SM8550 entry)
    - Test 6: DedupEngine.match() -> regex match uses extract_model from common.py
    - Test 7: DedupEngine.match() -> wikidata_qid match when chip has wikidata_id
    - Test 8: DedupEngine.match() -> fuzzy_name match with rapidfuzz token_sort_ratio >= 85
    - Test 9: DedupEngine.match() -> no_match for completely unrelated chip
    - Test 10: DedupEngine.match() strategy ordering: exact must be tried before alias, etc.
    - Test 11: rapidfuzz import failure gracefully degrades fuzzy strategy (mock ImportError)
  </behavior>
  <action>
    Create `tests/unit/test_dedup.py` with the following test classes:

    **class TestChipUUID:**
    - `test_deterministic`: Same inputs always return same UUID.
    - `test_format`: Returns 32-char hex string, all lowercase hex digits.
    - `test_case_insensitive`: Vendor/model case differences produce same UUID.
    - `test_different_inputs`: Different (vendor, model) produce different UUIDs.
    - `test_empty_model`: Falls back gracefully, produces valid 32-char hex.
    - `test_empty_vendor_and_model`: Falls back to slug(name, "") and returns valid hex.

    **class TestAliasRegistry:**
    - `test_known_aliases`: CHIP_ALIASES contains expected entries (e.g., "sm8550" with "kalama").
    - `test_lookup`: `_normalize_model("SM8550")` returns "sm8550" for alias lookup.

    **class TestDedupEngine:**
    - `test_exact_model_match`: Chip with model "SM8550" matches existing {"model": "SM8550"}.
    - `test_alias_match`: Chip with name "Kalama" matches existing entry with model "SM8550" (via alias).
    - `test_regex_match`: Chip with name "Snapdragon 8 Gen 2" and no model matches existing model "SM8550" via extract_model.
    - `test_wikidata_qid_match`: Chip with wikidata_id "Q123" matches existing entry with same wikidata_id.
    - `test_fuzzy_match`: Chip name "Snapdragn 8 Gen 2" (typo) matches "Snapdragon 8 Gen 2" with score >=85.
    - `test_no_match`: Completely unrelated chip (different vendor, model, name) returns None.
    - `test_strategy_ordering`: Mock each strategy to verify ordering exact->alias->regex->wikidata->fuzzy.
    - `test_fuzzy_threshold`: Below-threshold names do NOT match.
    - `test_fuzzy_different_vendor`: Same name, different vendor does NOT match.
    - `test_fuzzy_degradation`: When rapidfuzz import is mocked to fail, fuzzy returns no_match and logs warning.

    **class TestCanonicalID:**
    - `test_with_model`: Returns UUID5 string, not a slug.
    - `test_without_model`: Falls back to slug.
    - `test_backward_compatibility`: Chip dict has `id` (slug) and `uuid` (UUID5) as separate fields.

    **class TestDedupEdgeCases:**
    - `test_vendor_name_special_chars`: Vendor "NXP i.MX" with model "IMX8M" produces valid UUID.
    - `test_model_with_parentheses`: Model "EXYNOS 2200 (2022)" is properly normalized.
    - `test_empty_existing_dict`: match() returns (None, "no_match") for empty existing.
    - `test_chip_without_vendor`: Match returns None gracefully.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_dedup.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    25+ test cases in test_dedup.py. All pass. Edge cases covered (empty model, special chars, fuzzy degradation).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — write_vendor_file with DedupEngine + backward compatibility</name>
  <files>
    tests/unit/test_dedup.py (appended)
  </files>
  <behavior>
    - Test 1: write_vendor_file adds uuid to existing chips that lack it
    - Test 2: write_vendor_file deduplicates a new chip matching an existing one (exact model match)
    - Test 3: write_vendor_file merges fields for matched chips, prefers non-empty values
    - Test 4: write_vendor_file adds brand-new chips as separate entries
    - Test 5: enrich_one() adds uuid to chips that pass through it
    - Test 6: Backward compat: chip["id"] remains slug-based, is not overwritten by uuid
    - Test 7: All existing tests in test_common.py still pass
  </behavior>
  <action>
    Append the following test classes to `tests/unit/test_dedup.py`:

    **class TestWriteVendorFileIntegration:**
    Use `temp_data_dir` fixture and monkeypatch `soc_db.common.DATA_DIR` and `VENDOR_FILES` to point to temp dir.

    - `test_existing_chips_get_uuid`: Create a temp vendor file with 2 chips lacking uuid. Run write_vendor_file to read then re-write (with empty new chips list). Read back and verify both chips have 32-char hex uuid.
    - `test_new_chip_matches_exact_model`: Create temp vendor file with one chip. Call write_vendor_file with new chip that has same model but different name. Verify only one chip exists (matched), and its name is updated.
    - `test_new_chip_matches_fuzzy_name`: Create temp vendor file with "Snapdragon 8 Gen 2". Call write_vendor_file with "Snapdragn 8 Gen 2" (typo). Verify fuzzy match merges them.
    - `test_new_chip_no_match`: Create temp vendor file with one chip. Add a completely different chip. Verify both exist (2 entries).
    - `test_nonexistent_vendor_skipped`: Call write_vendor_file("NonExistent", [...]). Verify no error and no file created.
    - `test_completeness_pruning_preserved`: Verify that the stale-entry pruning logic (completeness < 0.28, GPU-only names) still works with DedupEngine — i.e., dedup doesn't interfere with existing pruning.

    **class TestEnrichUUID:**
    - `test_enrich_one_adds_uuid`: enrich_one() on a minimal chip produces uuid.
    - `test_enrich_one_preserves_existing_uuid`: If chip already has uuid, enrich_one doesn't overwrite it.
    - `test_enrich_all_adds_uuid`: enrich_all() on multiple chips adds uuid to all.

    **class TestBackwardCompatibility:**
    - `test_slug_id_preserved`: Verify that chip["id"] remains the slug-derived value, not the UUID.
    - `test_existing_slug_still_works`: A slug-based ID that exists in data is still returned by match() if model matches (via exact strategy).
    - `test_common_tests_still_pass`: Run a subset of existing test_common.py tests to verify no regression. This is implicit in the full test run.

    **class TestScriptsDeletion:**
    - `test_no_scripts_import`: Attempt `import scripts.common` and verify ImportError.
    - `test_scraper_apple_in_src`: `from soc_db.scraping.sources.apple import AppleScraper` works.
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/test_dedup.py tests/unit/test_common.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    write_vendor_file integration tests pass. enrich_one() adds uuid. Backward compat verified: slug IDs unchanged. All existing common tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression run — all existing tests must pass</name>
  <files>
    tests/ (no modification — run only)
  </files>
  <action>
    Run the full existing test suite and verify zero regressions:

    1. Run `python3 -m pytest tests/ -v --tb=short -x` to execute all tests.
    2. If any test fails, diagnose the root cause:
       - If failure is in tests/unit/test_common.py (e.g., write_vendor_file, _match_existing, enrich_one): the DedupEngine integration in Plan 1 may have changed behavior. Fix the test or adjust the implementation — but only if the change is logically correct (e.g., match behavior improved) and documented.
       - If failure is in tests/unit/test_scraping_base.py or test_scraping_*.py: the scripts/ deletion in Plan 2 may have removed an expected module. Verify all imports resolve.
       - If failure is in tests/validate.py or integration tests: verify data integrity.
    3. Report failed tests and fixes applied.

    **Key areas most likely affected by changes:**
    - `test_common.py`: `TestMatchExisting` — _match_existing now delegates to DedupEngine; verify behavior is equivalent or strictly better.
    - `test_common.py`: `TestWriteVendorFile` — any test calling write_vendor_file should still work because the dedup integration is additive (uuid field added, match logic enhanced).
    - Snapshot tests: `tests/snapshot/` — the uuid field will appear in every chip. Snapshot tests need their expected output updated to include the uuid field. Update the snapshot golden files to include uuid.

    4. **Snapshot update protocol:** If snapshot tests fail because uuid was added to all chips:
       - Run `python3 -m pytest tests/snapshot/ --snapshot-update` to auto-update golden files (if using pytest-snapshot or similar).
       - Or manually update the expected JSON to include uuid fields.
       - Or, if snapshots are stored as raw JSON, run the pipeline that generates them and re-commit.
    5. Run the full suite one final time. All tests must pass with exit code 0.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    Full test suite passes with exit code 0. uuid field added to snapshot golden files if needed. No regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> production code | Tests exercise all DedupEngine paths including fuzzy matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | Fuzzy match threshold at 85 may cause false positives | medium | mitigate | Test that below-threshold names do NOT match (TestDedupEngine.test_fuzzy_threshold) |
| T-10-04 | Information Disclosure | UUID exposes vendor+model fingerprint | low | accept | UUID is derived from public data (vendor, model) already exposed in chip records |
</threat_model>

<verification>
- python3 -m pytest tests/unit/test_dedup.py -v --tb=short -> all pass
- python3 -m pytest tests/ -v --tb=short -x -> all 405+ existing tests pass with exit code 0
- Backward compat verified: slug-based IDs unchanged, no API changes
</verification>

<success_criteria>
- 30+ test cases in test_dedup.py covering all matching strategies
- Integration tests verify write_vendor_file produces correct dedup behavior
- Full suite of 405+ existing tests passes with zero regressions
- uuid field added to any snapshot golden files
</success_criteria>

<output>
Create `.planning/phases/10-dedup-and-identity/10-03-SUMMARY.md` when done
</output>