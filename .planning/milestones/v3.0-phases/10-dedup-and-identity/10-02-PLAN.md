---
phase: 10-dedup-and-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/soc_db/scraping/sources/apple.py
  - src/soc_db/scraping/sources/linux_dt.py
  - src/soc_db/scraping/sources/wikidata.py
  - src/soc_db/scraping/__init__.py
  - src/soc_db/enrich/dts.py
  - src/soc_db/__init__.py
  - pyproject.toml
  - scripts/ (deleted)
autonomous: true
requirements:
  - DEDUP-03

must_haves:
  truths:
    - scripts/ directory no longer exists in the repo
    - All unique functionality from scripts/ exists in src/soc_db/scraping/ or src/soc_db/enrich/
    - Apple, Linux DT, and Wikidata scrapers are migrated to the BaseScraper framework
    - No imports reference scripts/ modules from any src/ code
    - pyproject.toml ruff exclude no longer references scripts/
  artifacts:
    - src/soc_db/scraping/sources/apple.py (new)
    - src/soc_db/scraping/sources/linux_dt.py (new)
    - src/soc_db/scraping/sources/wikidata.py (new)
    - src/soc_db/enrich/dts.py (new)
    - src/soc_db/scraping/__init__.py (updated exports)
    - pyproject.toml (updated)
  key_links:
    - AppleScraper, LinuxDTScraper, WikidataScraper registered in SourceRegistry
    - enrich_from_dts logic moved to enrichment module
    - All scripts/ files safely deleted after migration
---
<objective>
Consolidate and delete the legacy scripts/ directory, migrating unique functionality into the scraping framework.

**Purpose:** The scripts/ directory contains duplicated code (common.py is a stale copy of src/soc_db/common.py), legacy standalone scrapers that predate the BaseScraper framework, and orchestration scripts obsoleted by SourceRegistry. All non-redundant functionality is moved to `src/soc_db/scraping/sources/` and `src/soc_db/enrich/`. After migration, the entire scripts/ directory is deleted.

**Output:** 4 new source modules, updated package exports, deleted scripts/ directory.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@src/soc_db/scraping/base.py
@src/soc_db/scraping/sources/wikipedia.py
@src/soc_db/scraping/__init__.py
@src/soc_db/scraping/registry.py
@src/soc_db/scraping/source.py
@src/soc_db/common.py
@src/soc_db/__init__.py
@src/soc_db/wikidata.py
@src/soc_db/scraper_wikipedia.py
@src/soc_db/scraper_apple.py
@src/soc_db/dedup.py
@pyproject.toml
@scripts/common.py
@scripts/scraper_apple.py
@scripts/scraper_linux_dt.py
@scripts/scraper_wikidata_sparql.py
@scripts/scraper_wikidata.py
@scripts/enrich_from_dts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Apple, Linux DT, and Wikidata scrapers to BaseScraper framework</name>
  <files>
    src/soc_db/scraping/sources/apple.py
    src/soc_db/scraping/sources/linux_dt.py
    src/soc_db/scraping/sources/wikidata.py
    src/soc_db/scraping/__init__.py
    src/soc_db/__init__.py
    pyproject.toml
  </files>
  <action>
    Create `src/soc_db/scraping/sources/apple.py`:

    Migrate logic from `scripts/scraper_apple.py` and `src/soc_db/scraper_apple.py` into a `BaseScraper` subclass.

    ```python
    class AppleScraper(BaseScraper):
        SOURCE_ID = "apple"
        VENDORS = ["Apple"]
        PRIORITY = 40
        RATE_LIMIT_CONFIG = {"requests_per_sec": 1.0, "burst": 2, ...}
    ```

    - Import `APPLE_CHIPS` dict from `soc_db.scraper_apple` (the src/ version), reuse `parse_tables()`.
    - `fetch()` returns dict of {"A": html, "M": html}.
    - `parse()` uses BeautifulSoup + existing parse_tables(), returns `list[ChipScrapeResult]`.
    - `dedup()` does not need override (parent handles (vendor, model, name) dedup).
    - `write()` uses parent method (calls `write_vendor_file` which uses DedupEngine).

    Create `src/soc_db/scraping/sources/linux_dt.py`:

    Migrate `scripts/scraper_linux_dt.py` into a `BaseScraper` subclass.

    ```python
    class LinuxDTScraper(BaseScraper):
        SOURCE_ID = "linux_dt"
        VENDORS = [...]  # all VENDOR_MAP values as list
        PRIORITY = 60
        RATE_LIMIT_CONFIG = {"requests_per_sec": 1.0, "burst": 1, ...}
    ```

    - Move `VENDOR_MAP`, `VENDOR_SOC_PATTERNS`, `ARM32_VENDOR_DIRS`, `ARCH_PATHS`, `fetch_tree()`, `dts_to_soc_name()`, `dts_to_marketing_name()` into the module (renaming to module-level constants, not class-level).
    - `fetch()` calls `fetch_tree()` and returns the tree list.
    - `parse()` iterates tree entries, extracts SoC model numbers, returns `ChipScrapeResult` per chip. The original `scrape()` function logic becomes the parse method.
    - Use `from soc_db.scraping.source import HTTPSource` for HTTP fetching with rate limiting instead of `urllib.request.urlopen`. Use GitHub API v3 with proper User-Agent.
    - `write()` uses parent method.

    Create `src/soc_db/scraping/sources/wikidata.py`:

    Migrate `scripts/scraper_wikidata.py` and `scripts/scraper_wikidata_sparql.py` into a single `BaseScraper` subclass.

    ```python
    class WikidataScraper(BaseScraper):
        SOURCE_ID = "wikidata"
        VENDORS = [...]  # all known vendors
        PRIORITY = 70
    ```

    - Combine `KNOWN_MANUFACTURERS` from scraper_wikidata.py and `MANUFACTURERS` from scraper_wikidata_sparql.py into one dict.
    - Import `run_sparql` from `soc_db.wikidata` (which already has the SPARQL wrapper with backoff and caching).
    - `fetch()` runs SPARQL queries for all vendors, returns raw bindings per vendor.
    - `parse()` converts bindings into `ChipScrapeResult` list.
    - The `_build_query()` and `parse_chip()` logic from scraper_wikidata.py is adapted to work with the framework.
    - `write()` uses parent method.

    Update `src/soc_db/scraping/__init__.py`:

    - Import all new scrapers: `from soc_db.scraping.sources.apple import AppleScraper`, `from soc_db.scraping.sources.linux_dt import LinuxDTScraper`, `from soc_db.scraping.sources.wikidata import WikidataScraper`.
    - Add to `__all__`.

    Update `src/soc_db/__init__.py`:

    - Import and re-export the new scrapers in `__all__`.

    Update `pyproject.toml`:

    - **[CRITICAL per DEDUP-03, DEDUP-02]** Add `"rapidfuzz>=3.6"` to `[project] dependencies`.
    - Remove `"scripts/"` from `[tool.ruff] exclude`. Change from `exclude = ["scripts/", ".venv/"]` to `exclude = [".venv/"]`.
    - Remove `"scripts.*"` from `[[tool.mypy.overrides]]` module ignore list.

    **Comment-text discipline:** The literal text `scripts/` should NOT appear in any `<action>` body in a way that would be written into the source file (e.g., as a comment). The pyproject.toml edits remove `scripts/` from exclude lists — this is a legitimate configuration change, not a negative grep target.

    **Slop check:** rapidfuzz v3.6+ is a C-accelerated library with 1M+/week PyPI downloads and an established security record. `[ASSUMED]` per RESEARCH.md package audit.
  </action>
  <verify>
    <automated>python3 -c "
from soc_db.scraping.sources.apple import AppleScraper
from soc_db.scraping.sources.linux_dt import LinuxDTScraper
from soc_db.scraping.sources.wikidata import WikidataScraper
for cls in (AppleScraper, LinuxDTScraper, WikidataScraper):
    assert cls.SOURCE_ID != ''
    assert len(cls.VENDORS) > 0
    assert hasattr(cls, 'fetch') and hasattr(cls, 'parse')
    print(f'{cls.__name__}: SOURCE_ID={cls.SOURCE_ID}, VENDORS={len(cls.VENDORS)}')
print('All scrapers import cleanly')
"
  </verify>
  <done>
    3 new scraper modules import cleanly. Each has SOURCE_ID, VENDORS, fetch(), parse(). pyproject.toml includes rapidfuzz>=3.6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate enrich_from_dts.py to enrichment module + identify redundant scripts</name>
  <files>
    src/soc_db/enrich/dts.py
  </files>
  <action>
    Create `src/soc_db/enrich/dts.py` with the unique functionality from `scripts/enrich_from_dts.py`:

    1. **Imports:** `re`, `json`, `logging`, `collections.defaultdict`, `from soc_db.common import fetch, guard_path`, `from soc_db.scraping.source import HTTPSource`.

    2. **Architecture mapping** (`CORE_ARCH` list): Copy the (compatible_name, arch_string) mapping from scripts/enrich_from_dts.py lines 18-53. This is the primary value of this module.

    3. **VENDOR_DIR_MAP:** Copy the kernel vendor directory to our vendor name mapping from scripts/enrich_from_dts.py lines 55-72.

    4. **DTS parsing functions** (all module-level functions):
       - `get_dtsi_index()` — Reuse logic but use HTTPSource for GitHub API access with rate limiting
       - `extract_block(text, start)` — unchanged
       - `_detect_arch(compat)` — unchanged
       - `_resolve_include(inc_path, cur_dir)` — unchanged
       - `_parse_cpus_from_content(content, dtsi_path)` — unchanged
       - `parse_cpu_info(content, dtsi_path, _depth=0)` — unchanged
       - `enrich_from_dts(chip: dict) -> dict`: Public function that accepts a single chip dict, looks up its DTSI file, extracts CPU info (cores, architecture), and returns updated chip. This is the entry point for the enrichment pipeline.

    5. **Do NOT include** the `main()` function from the scripts version — that was a standalone CLI entry point. The module exposes `enrich_from_dts(chip)` for integration.

    The following scripts files are REDUNDANT and must be DELETED (all logic already in src/):
    - `scripts/common.py` — stale copy; src/soc_db/common.py is canonical
    - `scripts/parsers.py` — already at src/soc_db/parsers.py
    - `scripts/migrate.py` — trivial wrapper; obsoleted by soc-db CLI
    - `scripts/pipeline.py` — orchestration obsoleted by SourceRegistry
    - `scripts/run_all.sh` — obsoleted by framework automation
    - `scripts/extract_wikitables.py` — debug/exploration tool
    - `scripts/scraper_wikipedia.py` — already present at src/soc_db/scraper_wikipedia.py
    - `scripts/scraper_apple.py` — already present at src/soc_db/scraper_apple.py

    **Do NOT delete yet** — Task 3 handles the actual directory removal.
  </action>
  <verify>
    <automated>python3 -c "
from soc_db.enrich.dts import enrich_from_dts, CORE_ARCH, VENDOR_DIR_MAP
assert len(CORE_ARCH) >= 10
assert 'qcom' in VENDOR_DIR_MAP
print(f'enrich_from_dts imported: {len(CORE_ARCH)} arch mappings, {len(VENDOR_DIR_MAP)} vendor dirs')
"</automated>
  </verify>
  <done>
    src/soc_db/enrich/dts.py created with arch mappings, DTS parsing functions, and enrich_from_dts() entry point.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete scripts/ directory and fix remaining references</name>
  <files>
    scripts/ (deleted via git rm)
  </files>
  <action>
    Perform the final cleanup:

    1. **Verify scripts/ content is accounted for:**
       - `common.py` -> src/soc_db/common.py (already canonical)
       - `parsers.py` -> src/soc_db/parsers.py (already canonical)
       - `scraper_apple.py` -> src/soc_db/scraper_apple.py + scraping/sources/apple.py (Task 1)
       - `scraper_wikipedia.py` -> src/soc_db/scraper_wikipedia.py (already canonical)
       - `scraper_linux_dt.py` -> scraping/sources/linux_dt.py (Task 1)
       - `scraper_wikidata_sparql.py` -> scraping/sources/wikidata.py + soc_db/wikidata.py (Task 1)
       - `scraper_wikidata.py` -> scraping/sources/wikidata.py (Task 1)
       - `enrich_from_dts.py` -> soc_db/enrich/dts.py (Task 2)
       - `migrate.py` -> obsoleted (use soc-db CLI or framework directly)
       - `pipeline.py` -> obsoleted by SourceRegistry
       - `run_all.sh` -> obsoleted
       - `extract_wikitables.py` -> exploration tool, not migrated
       - `__pycache__/` -> transient, delete

    2. **Run `git rm -r scripts/`** to delete the directory from git tracking.

    3. **Check for stale references:**
       - Verify pyproject.toml no longer has `"scripts/"` in ruff exclude (updated in Task 1).
       - Verify pyproject.toml mypy override no longer has `"scripts.*"` (updated in Task 1).
       - Grep the src/ tree for any remaining `from scripts` or `import scripts` references.
       - Grep the tests/ tree for scripts/ references.
       - Fix any discovered references (there should be none).

    4. **Verify pyproject.toml** has `exclude = [".venv/"]` (without scripts/).

    5. **Git commit:** The deletion is committed with message `feat(10-dedup): remove legacy scripts/ directory per DEDUP-03`.
  </action>
  <verify>
    <automated>bash -c "
test ! -d 'scripts' || { echo 'ERROR: scripts/ still exists'; exit 1; }
echo 'scripts/ deleted OK'
python3 -c 'from soc_db.scraping.sources.apple import AppleScraper; from soc_db.scraping.sources.linux_dt import LinuxDTScraper; from soc_db.scraping.sources.wikidata import WikidataScraper; print(\"All migrations intact\")'
"</automated>
  </verify>
  <done>
    scripts/ directory no longer exists on disk or in git. All functionality is in src/soc_db/. No stale imports remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API -> LinuxDTScraper | Untrusted JSON from public API parsed into chip data |
| Wikidata SPARQL -> WikidataScraper | Untrusted SPARQL results parsed into chip data |
| Wikipedia -> AppleScraper | Untrusted HTML parsed via BeautifulSoup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | linux_dt, wikidata scrapers — network fetch | low | mitigate | Use HTTPSource with rate limiting (already in framework) |
| T-10-02 | Spoofing | Wikidata SPARQL results could contain malicious chip data | low | accept | Chip data is JSON, not executable. sanity-checked by enrich pipeline |
| T-10-SC | Tampering | rapidfuzz pip install | high | mitigate | [ASSUMED] per RESEARCH.md audit; blocking human checkpoint optional |
</threat_model>

<verification>
- All new scraper modules import without error
- pyproject.toml has rapidfuzz dependency, no scripts/ references
- scripts/ directory deleted from git
- grep -c 'from scripts\|import scripts' src/ tests/ returns 0
</verification>

<success_criteria>
- 3 new BaseScraper implementations (Apple, Linux DT, Wikidata) in scraping/sources/
- 1 new enrichment module (enrich/dts.py)
- scripts/ directory fully deleted
- No stale imports referencing scripts/ in src/ or tests/
</success_criteria>

<output>
Create `.planning/phases/10-dedup-and-identity/10-02-SUMMARY.md` when done
</output>